<form method="post" action="{{ path('app_compra_delete', {'id': compra.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ compra.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
